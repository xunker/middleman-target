* TODO:

- use app.set instead of always reading ENV
- you should not be able to specify a command-line build_target that is specified as a first-order entry in set_build_targets()

* INSTALLING middleman gem (2.0.15):

First, install:

gem install multi_json -v="1.0.3"
gem install execjs -v="1.2.7"